你是一个航天光学探测 + 机器学习方向的工程研究助理，请直接执行任务，不要给概念性方案，按照原有的模型代码的基础上修改代码，如果特征提取完之后发现数据量很少，使用gan扩充特征数据，没有的模块请写代码

## 项目目标
基于已有【推进器推力 / 质量流率随时间变化的数据】，构建一个：
**360 nm 紫外线辐射随时间变化的观测模型**，
并在此基础上研发一个**智能识别模型**，用于识别：
1) 点火时刻  
2) 是否发生变轨  
3) 推力大小  
4) 变轨类型  

## 已知前提（必须遵守）
- 推进器类型：肼（Hydrazine）单组元推进器
- 物理机理：化学发光（NH*），UV 强度 ∝ 质量流率 / 推力
- 使用一阶或弱非线性近似即可
- 本项目观测波段：360 nm（允许由 336 nm 特征近似）
- 不做复杂 CFD 或光谱仿真，只做“可用于智能识别的工程级建模”

## 输入数据
- thrust(t)：推力时间序列
- mfr(t)：质量流率时间序列
- ton(t)：点火指令
- 采样率约 100 Hz

## 第一阶段任务：推力 → 360 nm UV 映射
请你：
1. 设计一个 **可参数化的映射模型**
   - I_uv(t) = f(mfr(t)) 或 f(thrust(t))
   - 支持加入噪声、背景项
2. 给出 **明确的数学形式**
3. 给出 **Python 实现代码**
   - 输入：CSV（thrust / mfr）
   - 输出：新增列 `uv_360nm`

## 第二阶段任务：UV 特征工程（必须可计算）
基于生成的 `I_uv(t)`，提取以下特征：
- dI/dt（上升沿）
- 峰值强度
- 峰值持续时间
- 脉冲面积（积分）
- 脉冲数量与间隔
直接写代码，可以修改原有的特征提取代码的逻辑

## 第三阶段任务：识别模型构建
请分别实现以下模型（可用简化版本）：

### 1️⃣ 点火时刻识别
- 输入：UV 时间序列
- 输出：点火时间索引
- 方法：阈值 / CNN / dI/dt 判据

### 2️⃣ 是否变轨（二分类）
- 输入：单次事件 UV 序列
- 输出：是否变轨
- 模型：CNN / LSTM / RF（三选一即可）

### 3️⃣ 推力大小回归
- 输入：UV 特征
- 输出：推力估计值
- 模型：回归模型（RF / XGBoost）

### 4️⃣ 变轨类型分类
- 分类目标示例：
  - 短脉冲姿态修正
  - 长时低推变轨
  - 多脉冲调整
- 模型：多分类

## 输出要求（非常重要）
- 必须给：
  - 数学公式
  - Python 代码（可运行）
  - 每一步的输入 / 输出说明
- 默认我会直接把你的代码接入现有流水线测试

开始执行。
