1.轨道类型
2.传感器类型（检测得到想要的数据就行）
3.变轨检测模型（二选一）：
  随机森林（RF）：用多个决策树集成学习，降低过拟合，适合小样本，支持并行计算，快速处理残差特征。
  深度神经网络（DNN）：通过网格搜索（GSCV）优化隐藏层数量、批次大小等超参数，提升特征提取能力，减少虚警。
  点火时刻估计模型（二选一）：
  LSTM：处理长时序数据，解决梯度消失问题，通过回归 / 多标签分类定位变轨起始时刻。
  Transformer：用注意力机制并行处理数据，不用逐帧计算，提升时间戳估计速度。
  特征提取：从光学 / 雷达残差数据中提取 10 类特征（如残差均值、滑动窗口最大值等），区分噪声和变轨信号。
  优化后 DNN 在 GEO 光学数据上检测率 96.67%、虚警率 0.05%；RF 在 LEO 雷达数据上检测率 98.10%；LSTM 时间戳误差比传统方法降低 9.75%。
### 解决传统深度学习模型计算量大、时序特征提取低效的问题，提升小推力变轨的识别率。
算法原理（通俗版）：
核心模型（MLF-SNN）：基于脉冲神经网络（SNN），设计 “多阈值脉冲神经元（MLF）”—— 每个神经元有 3 个递增阈值（0.6、1.6、2.6），不同强度的轨道参数变化会触发不同阈值的脉冲，避免单一阈值导致的特征丢失。
训练技巧（surrogate 梯度）：SNN 的脉冲信号不可微分，用 “替代梯度” 方法近似计算梯度，解决训练时梯度消失的问题。
特征输入：提取轨道参数差异特征（如倾角差、半长轴差）和雷达截面积（RCS），转成脉冲序列输入模型
适合计算量少，实时性要求比较高的场景，地面站实时处理
#### 双轨道检测
算法原理
双轨道解算：同时计算 “简化动力学轨道”（基于物理模型预测的轨道）和 “运动学轨道”（纯 GNSS 观测数据解算的轨道）。
特征指标（几何距离差 RMS）：计算两个轨道到所有可见 GNSS 卫星的几何距离差异，取均方根（RMS）形成时序指标 —— 变轨时物理模型无法及时响应，两个轨道会 “脱节”，RMS 值突然飙升。
动态阈值：用滑动窗口的中位数和绝对偏差（MAD）构建阈值，自适应噪声变化，避免固定阈值导致的虚警 / 漏检。
核心结果：平均检测延迟 90 秒，能检测 Δv≥0.0124m/s 的小推力变轨；Sentinel-3A 卫星（815km 轨道）和 GRACE-FO 卫星（490km 轨道）上均验证有效，检测率 87.5%（8 个事件中成功检测 7 个）。
### 创新点总结：
#### 创新点 1：多维度特征融合策略，解决高噪声下特征区分度低的问题
现有不足：论文 1 仅用主成分特征，论文 7 仅用几何距离特征，单一特征易受噪声干扰，难以区分小推力变轨和噪声。
创新设计：融合 “时域 - 频域 - 物理特征” 三维向量 —— 时域（辐射强度峰值 P、持续时间 T）、频域（226nm/306nm 强度比 R）、物理域（轨道参数差异，如半长轴变化率），再用 HMSE（论文 5 的改进版）处理特征，增强噪声鲁棒性。
优势：比单一特征维度更全面，能精准捕捉小推力变轨的 “弱信号”，适配信噪比≥5dB 的场景。
#### 创新点 2：小样本适配的 DPC-1D CNN 优化模型，兼顾准确率和速度
现有不足：论文 1 的 1D CNN 未处理小样本问题，论文 2 的 RF 泛化能力有限，你的课题需要样本量提升 + 模型优化的双重保障。
创新设计：
用 GAN 网络扩充小样本（样本量提升 10 倍），解决数据稀缺问题；
改进密度峰值聚类（DPC）：先对扩充后的特征聚类，减少冗余数据，再输入 1D CNN；或者是使用dbscan两个会做一次的对比。
引入 PCA（论文 1 思路）对聚类后的数据做预处理，降低 1D CNN 计算复杂度，确保响应时间≤5 秒。
优势：比传统 1D CNN 准确率提升（≥92%），比 2D CNN 速度快，适配 GPU 并行计算。
#### 创新点 3：双阶段精准识别机制，同时定位点火时刻和推力大小
现有不足：论文 2/3 仅侧重 “检测 + 时间戳”，论文 7 仅检测变轨，均未实现推力大小的精准估计；传统方法无法同时满足两个目标。
创新设计：
第一阶段（点火时刻定位）：用滑动窗口动态阈值（论文 7 思路）处理 1D CNN 输出结果，精准锁定变轨起始时刻（误差≤1 秒）；
第二阶段（推力大小估计）：基于三维特征向量（P、T、R）训练回归分支，建立 “特征 - 推力” 映射关系，实现 Δv<0.1m/s 的小推力精准估计。
优势：突破现有论文 “单目标识别” 局限，同时满足点火时刻和推力大小的识别需求，虚警率≤3%。

### 数据集：
真实 GEO 光学残差数据（来自 18 SDS 合作共享）
数据内容：GEO 卫星的赤经（RA）、赤纬（DEC）残差数据（原始值 + 归一化值），含变轨事件标注（点火时刻、是否变轨），采样间隔 30s~1min；
模拟 LEO 雷达残差数据（实验室公开发布）
数据内容：LEO 卫星的方位角（AZ）、仰角（EL）、斜距（Ra）、距离率（RR）残差数据，模拟不同信噪比（SNR=3~10dB）场景，含变轨标签；
光学数据针对整个卫星的，可能不是针对于尾焰的。针对于尾焰的辐射数据
使用建议：
主数据集：MIT 林肯实验室的 “真实 GEO 光学残差数据 + 模拟 LEO 雷达残差数据”，优先使用真实数据构建小样本基础（100 个变轨事件），用模拟数据补充辐射强度和小推力梯度标注；
补充数据集：GFZ 的 GRACE-FO 数据集，提取其 Δv 精准标注（如 0.0124m/s、0.2645m/s），用于校准你的推力估计回归分支；
数据融合：将 MIT 数据的光学残差与模拟的辐射强度数据绑定（按时间戳对齐），生成 “时间戳 + 226nm 强度 + 306nm 强度 + 光学残差 + 点火时刻 +Δv” 的完整结构化数据，直接用于特征提取和 GAN 扩充。
MIT 林肯实验室官网（https://www.ll.mit.edu/）的 “Data & Software” 板块提交申请，说明研究用途（非商用免费），
通过 MIT 林肯实验室的 “Space Systems” 研究板块（https://www.ll.mit.edu/r-d/space-systems）下载，搜索 “Simulated Radar Residuals for LEO Maneuver Detection”，
辐射强度数据补充：若需真实辐射强度数据，可结合 NASA 的 “Solar Radiation and Climate Experiment (SORCE)” 数据集（https://sorce.gsfc.nasa.gov/），提取 226nm/306nm 波段的太阳辐射强度，再与卫星轨道数据关联模拟。
数据格式兼容性：上述所有数据集均支持结构化处理（如 TLE 数据可通过 Python 的astropy库解析，GFZ 的 GRACE-FO 数据可通过pandas读取），可直接提取轨道参数、残差特征，与“辐射强度三维特征（P、T、R）” 融合；
模拟生成 226nm/306nm 辐射强度数据（核心环节）
采用论文 2/3 提到的Monte-Carlo 模拟方法（与生成光学残差模拟数据的工具一致，保证一致性），生成逻辑贴合物理规律：
输入参数（从光学残差 / 变轨标注中提取）：
变轨时：推力大小 Δv（论文 2/3 的模拟数据含推力标注，或按残差突变幅度反推）、持续时间 T、轨道类型（LEO/GEO，影响辐射传播衰减）；
非变轨时：背景噪声水平（按 SNR=3~10dB 设置，与光学残差的噪声等级匹配）；
生成模型（基于物理关联性）：
辐射强度峰值 P：P = k×Δv（k 为比例系数，可通过少量真实辐射数据校准，或参考卫星推进系统的辐射特性文献，比如推力 Δv=0.1m/s 时，P=1000±50 counts）；
持续时间 T：与光学残差标注的变轨持续时间一致（比如残差突变持续 10 秒，辐射强度维持峰值的时间也为 10 秒）；
强度比 R=226nm/306nm：变轨时 R≈常数（如 1.2±0.1，基于卫星推进剂燃烧的光谱特性），非变轨时 R 随机波动但范围稳定（如 0.8~1.0）；
噪声叠加：在生成的辐射强度基础上，按设定的 SNR 添加高斯噪声（与光学残差的噪声分布一致，避免单一噪声占优）。
步骤 3：量化叠加与特征整合（形成可用数据）
叠加不是简单相加，而是 “特征维度扩充”，不改变原有光学残差数据，具体方式：
方式 1：新增特征列（推荐，适配你的三维特征提取）：在光学残差数据集的每一行（对应一个时间戳），新增两列 “226nm 辐射强度”“306nm 辐射强度”，直接填入模拟生成的值；
方式 2：融合计算（可选，增强关联性）：若需强化辐射与残差的关联，可计算 “辐射强度 - 残差融合值”（如 I_226×|RA 残差 |），但需保留原始辐射强度列用于提取 P、T、R；
最终数据格式：时间戳、RA 残差、DEC 残差、226nm 辐射强度、306nm 辐射强度、变轨标签、点火时刻、Δv，完全适配你的特征提取和 1D CNN 输入需求。
步骤 4：一致性验证（确保叠加有效）
时序一致性：检查辐射强度峰值时刻是否与光学残差突变时刻对齐（误差≤1 个采样间隔）；
特征一致性：变轨事件中，辐射强度的峰值 P、持续时间 T 应与 Δv 正相关（如 Δv 越大，P 越大、T 越长），非变轨事件中辐射强度稳定在背景水平，不出现虚假峰值；
噪声兼容性：叠加后的数据在 SNR≥5dB 时，辐射强度的信号 - 噪声比≥3（确保能提取到清晰的 P 和 T）。